package views

templ FormRow(label string, id string, hint string) {
	<div>
		<label for={ id } class="form-label">{ label }</label>
		{ children... }
		if hint != "" {
			<p id={ id + "-hint" } class="form-hint">{ hint }</p>
		}
	</div>
}
